class Game {
    constructor(title, src, tag, description, href) {
        this.title = title;
        this.src = src;
        this.tag = tag;
        this.description = description;
        this.href = href;
    }
}

export default Game;
